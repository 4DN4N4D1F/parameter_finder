# parameter_finder
parameter_finder is a Python tool that scans a given website and automatically extracts all URLs containing GET parameters. This is especially useful for bug bounty hunters, penetration testers, and developers who want to quickly discover endpoints that accept user input.
